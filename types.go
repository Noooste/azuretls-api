package api

import "github.com/Noooste/azuretls-api/common"

// Re-export types from common package for backwards compatibility
type ServerRequest = common.ServerRequest
type RequestOptions = common.RequestOptions
type ServerResponse = common.ServerResponse
type Cookie = common.Cookie
type ServerConfig = common.ServerConfig
type SessionManager = common.SessionManager
type ServerInterface = common.Server
